import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

export default new Vuetify({
  theme: {
    themes: {
      primary: '#252422',
      secondary: '#FFFCF2',
      accent: '#403D39',
      font: '#CCC5B9',
    },
  },
});
